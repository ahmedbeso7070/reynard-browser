diff --git a/third_party/libwebrtc/modules/video_capture/video_capture_defines.h b/third_party/libwebrtc/modules/video_capture/video_capture_defines.h
index f814e4e7f5e6..e7bb3858505e 100644
--- a/third_party/libwebrtc/modules/video_capture/video_capture_defines.h
+++ b/third_party/libwebrtc/modules/video_capture/video_capture_defines.h
@@ -26,6 +26,7 @@ enum { kVideoCaptureProductIdLength = 128 };   // Max product id length
 struct VideoCaptureCapability {
   int32_t width;
   int32_t height;
+  int32_t minFPS;
   int32_t maxFPS;
   VideoType videoType;
   bool interlaced;
@@ -33,6 +34,7 @@ struct VideoCaptureCapability {
   VideoCaptureCapability() {
     width = 0;
     height = 0;
+    minFPS = 0;
     maxFPS = 0;
     videoType = VideoType::kUnknown;
     interlaced = false;
@@ -42,6 +44,8 @@ struct VideoCaptureCapability {
       return true;
     if (height != other.height)
       return true;
+    if (minFPS != other.minFPS)
+      return true;
     if (maxFPS != other.maxFPS)
       return true;
     if (videoType != other.videoType)
